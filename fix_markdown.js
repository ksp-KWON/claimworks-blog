const fs = require('fs'); const path = require('path'); const dir = './src/content/posts'; const files = fs.readdirSync(dir).filter(f => f.endsWith('.md')); for (const file of files) { const filePath = path.join(dir, file); let content = fs.readFileSync(filePath, 'utf8'); let original = content; content = content.replace(/<red>(.*?)<\/red>/g, '**?? **'); content = content.replace(/<orange>(.*?)<\/orange>/g, '**?? **'); content = content.replace(/<green>(.*?)<\/green>/g, '**?? **'); content = content.replace(/<blue>(.*?)<\/blue>/g, '**?? **'); content = content.replace(/<purple>(.*?)<\/purple>/g, '**?? **'); content = content.replace(/`\n(\[.*?\])\n([\s\S]*?)\n`/gs, '> ****\n> '); content = content.replace(/- \[ \] /g, '- ? '); if (original !== content) { fs.writeFileSync(filePath, content); console.log('Fixed ' + file); } }
